@inject WorkMarketingNet.Localization.Core.ILocalizationService Localization

@functions{
	string T(string text, params object[] args) { return Localization.Translate(text, args); }
}

<section data-route="video-details">
	<h2 class="paper-font-display2">@T("Video")</h2>
	<wmn-video id="{{params.videoId}}"></wmn-video> @*params is route (url) parameters*@
</section>